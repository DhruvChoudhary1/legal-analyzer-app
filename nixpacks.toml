[providers]
python = "3.11"

[phases.setup]
cmds = ["pip install --upgrade pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "python main.py"

[variables]
FLASK_ENV = "production"
