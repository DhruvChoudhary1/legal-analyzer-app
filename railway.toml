[build]
builder = "nixpacks"

[nixpacks.setup]
providers = ["node", "python"]

[nixpacks.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r requirements.txt",
  "npm install"
]

[nixpacks.start]
cmd = "node app.js"